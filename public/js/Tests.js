function TestEnoughImages()
{
	// Write Test to determin there are enought images
}

function TestImageOrder()
{
	// Write test to determine if order is working properly. (forward and reverse);
	for (var i = 0; i < Slides.length; ++i)
	{
		//myAlert("TEST" + i + ": " + Slides[i].Image);
	}
}